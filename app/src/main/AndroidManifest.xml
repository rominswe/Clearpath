<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.clearpath.app">

    <!-- Permissions Section -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Clearpath"
        tools:targetApi="31">

        <!-- MainActivity (Launcher activity) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--  UserSignUpActivity -->
        <activity
            android:name=".UserSignUpActivity"
            android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>

        <!-- UserLoginActivity -->
        <activity
            android:name=".UserLoginActivity"
            android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>

        <!-- UserHomePageActivity -->
        <activity
        android:name=".UserHomePageActivity"
        android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>

        <!-- UserProfileInformationActivity -->
        <activity
            android:name=".UserProfileInformationActivity"
            android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>

        <!-- UserSOSCallingActivity -->
        <activity
            android:name=".UserSOSCallingActivity"
            android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>

        <!-- AssistantSignUpActivity -->
        <activity
            android:name=".AssistantSignUpActivity"
            android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>

        <!-- AssistantLoginActivity -->
        <activity
        android:name=".AssistantLoginActivity"
        android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>

        <!-- AssistantHomePageActivity -->
        <activity
            android:name=".AssistantHomePageActivity"
            android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>

        <!-- AssistantMonitoringActivity -->
        <activity
        android:name=".AssistantMonitoringActivity"
        android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>

        <!-- AssistantSOSCallingActivity -->
        <activity
            android:name=".AssistantSOSCallingActivity"
            android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>
        
        <!-- AssistantInformationActivity -->
        <activity
            android:name=".AssistantInformationActivity"
            android:exported="true">
            <!-- Optional Intent Filter can go here -->
        </activity>
        
    </application>

</manifest>
